{
  "provider": "openai_compatible",
  "base_url": "https://api.openai.com/v1",
  "model": "gpt-4.1-mini",
  "env": {
    "OPENAI_API_KEY": "set-this-in-your-shell",
    "OPENAI_BASE_URL": "optional-if-not-openai",
    "OPENAI_MODEL": "optional-if-default-is-not-used"
  },
  "notes": [
    "The filter is semantic, not keyword-based.",
    "If no provider is configured, fetched items are routed into the review bucket."
  ]
}
